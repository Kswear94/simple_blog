<footer class="footer">
  <nav>
    <ul>
      <% if(request.fullpath == posts_path) %>
          <li><%= link_to "New Post", new_post_path %></li>
        <% else if(request.fullpath == new_post_path) %>
          <li><%= link_to "Current Posts", posts_path %></li>
        <% else %>
          <li><%= link_to "Current Posts", posts_path %></li> 
          <li><%= link_to "New Post", new_post_path %></li>
        <% end %>
        <% end %>
    </ul>
  </nav>
</footer>